name: build

on:
  push:
    branches:
      - master

  pull_request:

jobs:
  build:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v5

      - name: Run Swift package tests
        run: swift test